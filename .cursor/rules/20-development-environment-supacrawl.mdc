---
Rule Type: Tooling
Scope: Project
Extends: 20-development-environment-basics (master)
ISO/IEC 25010:
  Primary: N/A (Tooling rule)
description: Supacrawl-specific development environment setup - conda environment and directory structure
globs: "**/*"
alwaysApply: true
---

# Development Environment

This rule extends `.cursor/rules/master/20-development-environment-basics.mdc` with supacrawl-specific patterns. Universal conda environment and environment variable patterns are covered in the master rule.

## Conda Environment

- ✅ **Must** use `supacrawl` conda environment (see `.cursor/rules/master/20-development-environment-basics.mdc` for activation requirements)
- ✅ **Must** activate conda environment before running CLI commands, tests, or installing packages
- ✅ **Must** install all dependencies from `pyproject.toml` using `pip install -e .`
- ✅ **Must** use conda environment for all development work (ruff, mypy, pytest)

## Directory Structure

- ✅ **Must** keep CLI modules in `src/supacrawl/cli/` directory
- ✅ **Must** keep service modules in `src/supacrawl/services/` directory
- ✅ **Must** keep LLM provider modules in `src/supacrawl/llm/` directory
- ✅ **Must** use cache directory at `.supacrawl/cache/` for scraped content

## Environment Variables

- ✅ **Must** use `.env` file for local development configuration
- ✅ **Must** document all required environment variables in README
- ✅ **May** set `SUPACRAWL_*` environment variables to configure browser behaviour (headless mode, locale, timezone, user agent, wait conditions, SPA delay)
- ✅ **May** set `OLLAMA_HOST` for Ollama server URL
- ✅ **May** set `OPENAI_API_KEY` for OpenAI API access
- ✅ **May** set `ANTHROPIC_API_KEY` for Anthropic API access
- ✅ **May** set `BRAVE_API_KEY` for Brave Search API access
- ❌ **Must NOT** commit `.env` files to version control
- ❌ **Must NOT** hardcode API keys or URLs in code

## References

- `.cursor/rules/master/20-development-environment-basics.mdc` - Universal development environment mechanics
- `.cursor/rules/50-scraper-provider-patterns-supacrawl.mdc` - Playwright scraper patterns
